[pytest]
markers =
    external: tests calling live external APIs
    slow:     tests taking more than 5 seconds
    openai:   tests using the OpenAI API

# Apply multiple markers to one test
@pytest.mark.external
@pytest.mark.slow
def test_github_api():
    resp = requests.get("api.github.com")
    assert resp.status_code == 200
